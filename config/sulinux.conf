# Sulinux építési beállítások (a build.sh olvassa)

DISTRO_NAME="Sulinux"
DISTRO_VERSION="13"            # a Debian főverziója, amire épül (13 = trixie)


# 1 = újabb kernel a trixie-backports tárolóból (a legújabb laptopokhoz,
#     pl. friss Wi-Fi-/grafikus chipekhez). 0 = a stabil Debian kernel.
BACKPORTS_KERNEL=1
